# USB bench/debug overlay. Build with:
#   west build ... -DOVERLAY_CONFIG=prj_uart.conf
#
# Keep the default prj.conf battery-safe for standalone use.
CONFIG_SERIAL=y
CONFIG_PRINTK=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_STDOUT_CONSOLE=y
